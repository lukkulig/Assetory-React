const styles = theme => ({
    root: {
        display: 'flex',
    },
    content: {
        flexGrow: 1,
        float: 'center',
        textAlign: 'center',
        padding: theme.spacing.unit * 3,
    },
});

export default styles
